# 2026-08-17

## block/buzz 검토

- 오딘이 GitHub 저장소 `https://github.com/block/buzz`를 공유하며 우리 팀원으로 참여시켜도 될 아이템인지 판단을 요청했다.
- 확인 시점 기준 Buzz는 Block의 Apache-2.0 공개 저장소이며, "humans and agents build together, on a relay you own"를 표방하는 self-hostable workspace/relay 플랫폼이다.
- 주요 구조: Nostr 기반 signed event log, Rust relay, Tauri/React desktop, Flutter mobile, `buzz-cli`, ACP/MCP agent harness, Postgres/Redis/S3 또는 MinIO 기반.
- 긍정 요소: 에이전트를 봇이 아니라 독립 키와 audit trail을 가진 멤버로 취급하는 설계가 OpenClaw/셀르메스 방향과 잘 맞음. 개발 활동과 릴리스 속도도 매우 활발함.
- 리스크: 0.5.x pre-1.0, 열린 이슈/PR 규모가 크고, 로컬/운영 스택이 가볍지 않음. 바로 실운영 핵심에 편입하지 말고 격리 PoC 후보로 보는 것이 적절함.
- 권고: 정식 팀원/인프라 편입은 보류. 공개/합성 데이터만 쓰는 1주 PoC로 로컬 relay, agent 1명, 채널 메시지/검색/audit/중단조건을 검증한 뒤 재판단.

## block/buzz 격리 PoC 시작

- 오딘이 "제안대로 진행해줘"라고 지시해 `/Users/mac/clawd/poc/buzz`에 `block/buzz`를 shallow clone했다.
- 공식 `scripts/start-isolated-test-relay.sh --profile dev`를 사용해 `buzz-harness` Compose project로 격리 relay를 실행했다.
- 실행 포트: relay `http://localhost:3030`, health `8088`, metrics `9202`, Postgres `5471`, Redis `6471`, MinIO `9471`.
- `buzz-cli`와 `buzz-admin`을 빌드하고 합성 메시지 smoke test를 수행했다.
- 결과: channel create, message send/get/thread, full-text search 성공. DB 기준 `events=34`, `audit_log=32` 확인.
- Obsidian 브리프 저장: `/Users/mac/.openclaw/Obsidian_Vault/3_Resources/2026-08-17-block-buzz-openclaw-poc-brief.md`.
- 다음 검증 후보: `buzz-acp` 또는 `buzz-agent`로 agent 1명 연결, private channel 권한/검색 격리, timeout/heartbeat 중단조건 확인.

## block/buzz PoC 다음 단계 승인 및 v0.2 검증

- 오딘이 "다음 단계 진행"을 승인했다.
- `buzz-acp`를 빌드하고, 비용 없는 `BUZZ_ACP_LAZY_POOL=true` 방식으로 agent identity 연결을 검증했다.
- private channel `31016431-dd6e-4f8b-9008-357d29b61ff0` 생성 후 agent pubkey `8f01c521ae0075458f52d9ff6699ac1426f750d9b3234d77b0b0d5f6bef8687a`를 bot role로 추가했다.
- owner/agent는 private 합성 메시지 조회와 full-text search 결과를 볼 수 있었고, outsider는 private channel list/messages/search 모두 빈 결과였다.
- `buzz-acp` 로그에서 NIP-42 인증 성공, `discovered 1 channel(s)`, private channel subscribe, online presence accepted 확인.
- DB 카운트는 `events=47`, `audit_log=41`까지 증가했다.
- 실제 LLM/ACP child 실행은 API key·비용·외부 호출 가능성이 있어 다음 별도 승인 범위로 남겼다.
- 오딘은 Buzz를 "정식 승격 전 건강검진/검증 단계로 계속 써보자"는 뜻으로 이해했고, 민호도 그 해석에 동의했다. 이후 안정성, 권한 경계, agent 응답 흐름, 운영 부담을 확인한 뒤 통과 시 정식 승격 제안을 올리는 흐름으로 정리.
